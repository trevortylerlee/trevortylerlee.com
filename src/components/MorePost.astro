---
import { formatDate } from '../js/utils';

const darkBg = '#333';
const lightBg = '#fff';
const { date, href, title, description, last } = Astro.props;
---

<div class={`${last ? 'last' : ''} container`}>
  <a href={`/posts/${href}`}>
    <div class="wrapper">
      <div class="post">
        <div class="title">
          <h4>{title}</h4>
        </div>
        <div class="date">
          <p class="date">{formatDate(date)}</p>
        </div>
      </div>
      { description && <p class="desc">{description}</p>}
    </div>
  </a>
</div>

<style define:vars={{ lightBg, darkBg }}>
  p {
    margin: 0;
    padding: 0;
  }

  a {
    color: inherit;
  }

  a:hover .wrapper {
    outline: 2px dotted var(--primary-color);
  }

  a:focus .wrapper {
    outline: 2px dotted var(--primary-color);
  }

  a:hover p {
    box-shadow: none;
    text-shadow: none;
  }

  a:focus p {
    box-shadow: none;
    text-shadow: none;
  }

  .wrapper {
    border-radius: 8px;
    padding: 0.75rem;
  }

  .post {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .date {
    font-size: 0.75rem;
    opacity: 0.75;
  }

  .last {
    margin-bottom: 4rem;
  }

  .container {
    border-radius: 0.5rem;
  }

  .container:hover,
  .container:focus {
    background: #e9e9e9;
  }

  :global(.dark)
  .container:hover,
  .container:focus {
    background: #333333;
  }

  .title {
    width: 70%;
  }

  .title h4 {
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
    font-size: 1.125rem;
    font-weight: 600;
  }

  .date {
    min-width: 25%;
    text-align: right;
    white-space: nowrap;
  }

  .desc {
    font-size: 0.875rem;
    margin-top: 0.5rem;
  }
</style>
