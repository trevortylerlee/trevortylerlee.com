---
const { src, type } = Astro.props;
---

<div class="videoWrapper">
  <video width="320" loop autoplay controls>
    <source src={src} type={`video/${type}`} />
    Your browser does not support the video tag.
  </video>
</div>

<style>
  .videoWrapper {
    display: flex;
    justify-content: center;
  }
</style>
